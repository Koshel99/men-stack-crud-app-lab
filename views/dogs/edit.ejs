<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= dog.name %></title>
  </head>
  <body>
    <h1>Editing <%= dog.name %>!</h1>

    <form action="/dogs/<%= dog._id %>?_method=PUT" method="POST">
      <label for="name">Name:</label>
      <input
        type="text"
        name="name"
        id="name"
        value="<%= dog.name %>"
      />

      <label for="breed">Breed:</label>
      <input
        type="text"
        name="breed"
        id="breed"
        value="<%= dog.breed %>"
      />

      <label for="age">Age:</label>
      <input
        type="number"
        name="age"
        id="age"
        value="<%= dog.age %>"
      />

      <label for="isAdopted">Adopted?</label>
      <input
        type="checkbox"
        name="isAdopted"
        id="isAdopted"
        <% if(dog.isAdopted){ %>
          checked
        <% } %>
      />

      <button type="submit">Edit <%= dog.name %></button>
    </form>

    <a href="/dogs/<%= dog._id %>">Back to <%= dog.name %></a>
  </body>
</html>
